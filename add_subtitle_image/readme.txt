to run it on files:

python script.py --directory ./images --pattern "*.png" --style strip --start 1

goes to the directory images
only usess .png files, 
starts the number at 1

automatically sorts by name
